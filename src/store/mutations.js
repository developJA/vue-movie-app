export default {
    SET_DAILY(state, dailys) {
        state.dailys = dailys;
    },
    SET_WEEKLY(state, weeklys) {
        state.weeklys = weeklys;
    },
    SET_SEARCH(state, search) {
        state.search = search;
    }
}